# Environment Variables Documentation

## Required Environment Variables

### Backend (Lambda Function)
- `NODE_ENV`: "production"
- `DATABASE_URL`: PostgreSQL connection string (auto-generated by Terraform)
- `JWT_SECRET`: Secret key for JWT token signing
- `JWT_EXPIRES_IN`: Token expiration time (default: "7d")
- `PORT`: Server port (default: "3001")
- `CORS_ORIGIN`: Frontend URL for CORS (default: Vercel URL)

### Frontend (Vercel)
- `VITE_API_BASE_URL`: AWS API Gateway URL

## Environment Variables Status ✅

All required environment variables are now configured in the Terraform deployment:

1. **Database**: `DATABASE_URL` - Auto-generated from Aurora cluster
2. **Authentication**: `JWT_SECRET` - From terraform.tfvars
3. **CORS**: `CORS_ORIGIN` - Set to your Vercel frontend URL
4. **Server**: `PORT`, `NODE_ENV` - Set for production
5. **Frontend**: `VITE_API_BASE_URL` - Needs to be set in Vercel after deployment

## Missing Variables Fixed ✅

- Added `PORT` environment variable
- Added `CORS_ORIGIN` environment variable
- Recreated `terraform.tfvars.example` file

## Next Steps

1. Copy `terraform.tfvars.example` to `terraform.tfvars`
2. Set secure values for `db_password` and `jwt_secret`
3. Run deployment: `npm run deploy:aws`
4. Update Vercel with the API Gateway URL
